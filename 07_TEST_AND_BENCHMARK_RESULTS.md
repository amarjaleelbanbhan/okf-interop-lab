# Executed tests and observations

Date: 2026-09-23. Python 3.12.14. Pinned OKF HEAD: `ad30107c31c06aec8a7d5636e0d1058118604e6f`. Command: `PYTHONPATH=okf-interop-lab python -m unittest discover -s okf-interop-lab/tests -v` → **3 passed, 0 failed** after correcting one mistaken test expectation during development. Command: `python okf-interop-lab/interop.py okf-interop-lab/examples/bundle --upstream upstream-okf > okf-interop-lab/raw-results.json` → completed. Raw result: `raw-results.json`. Among three synthetic concept files, one expected edge was omitted by the pinned upstream extractor; missing=1, extra=0. The mixed-form fixture did not expose the omission because the relative link generated the same edge. No retrieval quality, latency, resource, citation, or real-world compatibility benchmark was run. No dataset beyond the checked-in synthetic fixture was used.

## Follow-up local revision (2026-09-24)

After replacing the regex link scanner with `markdown-it-py` CommonMark parsing and fixing frontmatter extraction, executed `python -m unittest discover -s tests -v` in the local environment (Python 3.11+ compatible code, installed `markdown-it-py` 4.2.0): **9 passed, 0 failed**. The added tests cover fenced/inline-code false positives, body horizontal rules, reference links, image exclusion, encoded space/backslash handling, and out-of-bundle source rejection. Executed `python interop.py examples/bundle > raw-results-local-v2.json` successfully. This is local unit-test evidence only; the new version has **not** been re-compared to a pinned upstream checkout or to additional consumers. The historical `raw-results.json` still documents the original baseline from 2026-09-23. CI has been configured but not run on GitHub. No retrieval or ecosystem-wide interoperability benchmark was performed.
