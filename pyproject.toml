[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "okf-interop-lab-prototype"
version = "0.1.0.dev0"
description = "Narrow OKF concept-link interoperability fixture (experimental)"
requires-python = ">=3.11"
dependencies = ["markdown-it-py>=3,<5"]

[tool.setuptools]
py-modules = ["interop"]
